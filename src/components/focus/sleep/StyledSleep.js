import styled from "styled-components";

export const StyledSleepHeader = styled.div`
  display: flex;
  justify-content: center;
  width: 100%;

  .csv {
    margin-right: auto;
    cursor: pointer;
  }

  .heading {
    margin: auto;
    font-style: italic;
  }

  .info {
    margin-left: auto;
  }
`;
